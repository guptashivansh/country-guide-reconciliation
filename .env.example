GROQ_API_KEY=
EXTRACTION_CHUNK_SIZE=6000
OFFICIAL_SOURCES_JSON_URL=https://raw.githubusercontent.com/guptashivansh/compliance-data/main/data/official-sources.json

# Database backend — leave unset (or empty) to use SQLite (country_guides.db).
# Set to a PostgreSQL connection string to use Postgres instead.
# Requires: pip install psycopg2-binary
# DATABASE_URL=postgresql://user:password@localhost:5432/country_guides
